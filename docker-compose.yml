services:
  kin-surge:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      KIN_JWT_SECRET: change-this-in-production
      KIN_ALLOW_ORIGINS: http://localhost:8080
    volumes:
      - ./docker-data:/data
